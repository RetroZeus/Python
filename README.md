# Python
Folder Bomb attack

three modules : os,string,random

functions : 3
FUNCTIONS :
  connect_characters()
  create_folder()
  already_exists()

create_folder : as the name, it will create a folder, it takes one parameter called 'name', this will create a folder with it's name
already_exists : this is executed when create_folder throws an exception called 'FileExistsError'. create a random folder with a name made by python itself
connect_characters : this will create a random name for the folder, this function is used by already_exists.

# Done

INSTAGRAM : @ig.cyber_retro 
EMAIL : retrozeus5@gmail.com
